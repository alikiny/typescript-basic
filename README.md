# TypeScript Assignments

Check the questions in index.ts

### To run the project
1. `npm install`: install all the dependencies
2. `npm start` or `yarn start`: compile the ts file, form js file and run the code
3. You can use `console.log()` to see the result in your terminal
